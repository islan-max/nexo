name: CI/CD

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: ritmo
          POSTGRES_PASSWORD: ritmo_test
          POSTGRES_DB: ritmo_test
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
        ports:
          - "5432:5432"

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt
      - run: python -m ruff check .
      - run: python -m bandit -r app -ll -q
      - name: Run tests
        env:
          DATABASE_URL: postgresql://ritmo:ritmo_test@localhost:5432/ritmo_test
          TEST_DATABASE_URL: postgresql://ritmo:ritmo_test@localhost:5432/ritmo_test
          JWT_SECRET_KEY: ci-secret-key-for-testing-only-32chars
          ENVIRONMENT: testing
          ALLOWED_ORIGINS: http://localhost:8000
        run: pytest tests/ -q --cov=app --cov-report=term-missing --cov-report=xml --cov-fail-under=60
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Frontend checks
        working-directory: frontend
        run: |
          npm ci
          npm run typecheck
          npm run lint
          npm run build

  security-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install bandit pip-audit
      - run: bandit -r app/ -ll -q
      # pip-audit substitui o safety check (CLI descontinuado) para auditoria de deps.
      #
      # PYSEC-2026-1325 (ecdsa 0.19.2, dependência transitiva de python-jose) é
      # ignorada: não existe versão corrigida publicada e a falha é em operações
      # ECDSA, enquanto os tokens aqui são assinados com HS256 (HMAC) — o caminho
      # vulnerável nunca é executado. Reavaliar quando o ecdsa publicar correção,
      # ou ao migrar de python-jose para PyJWT.
      - run: pip-audit -r requirements.txt --strict --ignore-vuln PYSEC-2026-1325

  build-and-push:
    needs: [test, security-scan]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      # O driver `docker` padrão não exporta cache; sem o buildx o passo de build
      # falha com "Cache export is not supported for the docker driver" por causa
      # do cache-to: type=gha abaixo.
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=sha-
            type=raw,value=latest
      - uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
